# Cloudflare Tunnel Configuration
# Get your tunnel token from: https://dash.cloudflare.com/
# Navigate to Zero Trust > Access > Tunnels > Create/Edit Tunnel

# Option 1: Use tunnel token (recommended for new setups)
TUNNEL_TOKEN=your_tunnel_token_here

# Option 2: Use tunnel credentials file (for existing tunnels)
# TUNNEL_ID=your_tunnel_id_here
# TUNNEL_CREDENTIALS_FILE=/etc/cloudflared/credentials.json

# Optional: Custom tunnel name
# TUNNEL_NAME=gpu-dashboard-tunnel

# Optional: Tunnel metrics port (default: 2000)
# TUNNEL_METRICS_PORT=2000